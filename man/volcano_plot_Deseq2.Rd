% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deg-plots.R
\name{volcano_plot_Deseq2}
\alias{volcano_plot_Deseq2}
\title{Draw a volcano plot from DESeq2 results}
\usage{
volcano_plot_Deseq2(
  deseq2_result.df,
  gene.list,
  n = 5000,
  pc = FALSE,
  pv = 0.05,
  fc = 1.5,
  max_x = 100,
  max_y = 100,
  top = 5,
  adjust
)
}
\arguments{
\item{deseq2_result.df}{A data frame containing at least \code{Symbol},
\code{log2FoldChange}, \code{pvalue}, and \code{padj} columns. A \code{Gene_Type} column is
also required when \code{pc = TRUE}.}

\item{gene.list}{A character vector of gene symbols that should be retained
and labelled in the plot.}

\item{n}{Number of nonsignificant background genes sampled for plotting.}

\item{pc}{Logical; if \code{TRUE}, restrict candidate genes to those with
\code{Gene_Type == "protein_coding"}.}

\item{pv}{P-value or adjusted P-value threshold used to define significance.}

\item{fc}{Fold-change threshold on the linear scale. Vertical reference lines
are drawn at \verb{+/-log2(fc)}.}

\item{max_x}{Maximum absolute log2 fold change displayed. More extreme values
are capped at this limit.}

\item{max_y}{Maximum displayed \code{-log10} significance value.}

\item{top}{Number of the most significant genes to label in each fold-change
direction.}

\item{adjust}{Logical; use \code{padj} when \code{TRUE} and \code{pvalue} when \code{FALSE}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Creates a volcano plot from a DESeq2 result table, classifies significantly
upregulated and downregulated genes, and labels selected genes together with
the most significant genes in each direction. The plot is still returned
when either or both DEG directions are empty; counts are reported in a
message and in the plot subtitle.
}
\seealso{
\code{\link[=target_for_volcano]{target_for_volcano()}}
}
