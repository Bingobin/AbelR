% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survival.R
\name{plot_logistic_forest}
\alias{plot_logistic_forest}
\title{Plot odds ratios from logistic regression}
\usage{
plot_logistic_forest(
  data,
  outcome,
  predictors,
  term_labels = NULL,
  conf_level = 0.95,
  digits = 2,
  point_color = "firebrick",
  title = NULL,
  show_labels = TRUE
)
}
\arguments{
\item{data}{A data frame containing the outcome and predictor variables.}

\item{outcome}{Character scalar naming a binary outcome column. Numeric
outcomes must be coded as \code{0} and \code{1}; logical and two-level factor
outcomes are also supported.}

\item{predictors}{Character vector naming predictor columns.}

\item{term_labels}{Optional named character vector used to relabel model
coefficient terms. Names must be coefficient names, including factor
levels where applicable, and values are the labels displayed in the plot.}

\item{conf_level}{Confidence level for the Wald confidence intervals.}

\item{digits}{Number of decimal places used in plot labels.}

\item{point_color}{Color used for points and confidence intervals.}

\item{title}{Optional plot title. By default, the outcome name is shown.}

\item{show_labels}{Logical; if \code{TRUE}, display odds ratios, confidence
intervals, and significance symbols beside the estimates.}
}
\value{
A list containing \code{model}, the fitted \code{\link[stats:glm]{stats::glm()}}
object; \code{or}, a data frame of coefficient-level odds ratios and confidence
intervals; and \code{plot}, the forest plot.
}
\description{
Fits a multivariable binary logistic regression model, extracts odds ratios
with Wald confidence intervals, and draws a forest plot. Predictor names and
display labels are supplied by the caller, so the function can be used with
UK Biobank data or other cohort data.
}
\examples{
fit <- plot_logistic_forest(
  data = mtcars,
  outcome = "am",
  predictors = c("wt", "hp"),
  term_labels = c(
    wt = "Weight (1000 lbs)",
    hp = "Horsepower"
  )
)
fit$or
fit$plot
}
