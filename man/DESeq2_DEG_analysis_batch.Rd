% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deseq2.R
\name{DESeq2_DEG_analysis_batch}
\alias{DESeq2_DEG_analysis_batch}
\title{Run a filtered two-group DESeq2 analysis}
\usage{
DESeq2_DEG_analysis_batch(
  count.matrix,
  tpm.matrix = NULL,
  design.df,
  library = NULL,
  batch = NULL,
  tr = NULL,
  ctr = NULL,
  tr_filter = NULL,
  ctr_filter = NULL,
  tr_name = NULL,
  ctr_name = NULL,
  species = c("human", "mouse"),
  gene_anno_file = NULL
)
}
\arguments{
\item{count.matrix}{Count data frame with a \code{GID} column and sample columns.}

\item{tpm.matrix}{Optional TPM data frame using the same \code{GID} and sample
columns as \code{count.matrix}.}

\item{design.df}{Sample metadata whose row names match matrix sample columns.
It must contain \code{Group}, \code{Batch}, and \code{Library}.}

\item{library, batch, tr, ctr}{Values selecting one library, batch, treatment
group, and control group when custom filters are not supplied.}

\item{tr_filter, ctr_filter}{Optional named lists defining treatment and
control samples from arbitrary columns in \code{design.df}.}

\item{tr_name, ctr_name}{Optional display names for the resulting contrast
groups.}

\item{species}{Species used for annotation and ID conversion; \code{"human"} or
\code{"mouse"}.}

\item{gene_anno_file}{Optional annotation table (\code{.txt} or \code{.txt.gz}). When
\code{NULL}, AbelR uses its bundled species-specific annotation.}
}
\value{
A list containing DESeq2 coefficient names (\code{inter}), the annotated
result table (\code{result}), the variance-stabilized object (\code{vsd}), selected
sample metadata (\code{design}), and normalized \code{species}.
}
\description{
Selects treatment and control samples from a shared design table, fits a
DESeq2 model, joins human or mouse gene annotation and optional TPM values,
and calculates a variance-stabilized expression object.
}
