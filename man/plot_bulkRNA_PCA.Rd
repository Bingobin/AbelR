% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deseq2.R
\name{plot_bulkRNA_PCA}
\alias{plot_bulkRNA_PCA}
\title{Plot PCA for merged bulk RNA-seq batches}
\usage{
plot_bulkRNA_PCA(
  bulk_data,
  id_col = "GID",
  title = "PCA of bulk RNA-seq samples",
  color_by = "Group",
  shape_by = "CellLine",
  alpha_by = "Batch",
  alpha_values = NULL,
  point_size = 3.5,
  label_samples = TRUE,
  label_size = 2.5,
  blind = TRUE,
  top_var_n = 5000,
  cluster_heatmap = TRUE,
  annotation_columns = c("Group", "Library", "Batch"),
  heatmap_title = "Sample-to-sample distance clustering",
  interactive = FALSE
)
}
\arguments{
\item{bulk_data}{List returned by \code{\link[=build_bulkRNA_batches]{build_bulkRNA_batches()}} containing
\code{count} and \code{design}.}

\item{id_col}{Gene-identifier column in \code{bulk_data$count}.}

\item{title}{Plot title.}

\item{color_by, shape_by, alpha_by}{Design-table columns mapped to point color,
shape, and transparency.}

\item{alpha_values}{Optional named numeric vector defining transparency for
each \code{alpha_by} level. When \code{NULL}, values from 0.6 to 1 are generated
automatically.}

\item{point_size}{Point size.}

\item{label_samples}{Logical; label points with sample IDs.}

\item{label_size}{Sample-label size.}

\item{blind}{Logical passed to
\code{\link[DESeq2:varianceStabilizingTransformation]{DESeq2::varianceStabilizingTransformation()}}.}

\item{top_var_n}{Number of genes with the highest VST variance used for both
PCA and sample-distance clustering. Use \code{NULL} to retain all genes.}

\item{cluster_heatmap}{Logical; calculate a sample-distance heatmap using the
same genes as PCA.}

\item{annotation_columns}{Design-table columns displayed on the heatmap.}

\item{heatmap_title}{Sample-distance heatmap title.}

\item{interactive}{Logical; also create a Plotly version of the PCA plot.
This requires the optional \code{plotly} package.}
}
\value{
A list containing the ggplot (\code{plot}), optional Plotly widget
(\code{interactive_plot}), sample-distance heatmap (\code{heatmap}), distance matrix
(\code{sample_distance_matrix}), \code{dist} object (\code{sample_distance}), selected
genes (\code{selected_genes}), sample coordinates (\code{data}), explained variance
(\code{variance}), the \code{prcomp} result (\code{pca}), and the transformed DESeq2 object
(\code{vsd}).
}
\description{
Uses the count and design tables returned by \code{\link[=build_bulkRNA_batches]{build_bulkRNA_batches()}},
applies the DESeq2 variance-stabilizing transformation, calculates sample
principal components, and draws a batch-aware PCA plot.
}
