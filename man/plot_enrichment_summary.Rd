% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment.R
\name{plot_enrichment_summary}
\alias{plot_enrichment_summary}
\title{Plot grouped over-representation enrichment results}
\usage{
plot_enrichment_summary(
  enrichment_result,
  database = NULL,
  p_column = c("p.adjust", "pvalue"),
  pathway_column = c("ID", "Description"),
  top_n = 20,
  label_pathways = NULL,
  exclude_pathways = NULL,
  group_order = NULL,
  sentence_case = FALSE,
  low_color = "grey90",
  high_color = "#00441B",
  na_color = "grey85",
  max_logp = NULL
)
}
\arguments{
\item{enrichment_result}{A data frame, a \code{clusterProfiler} enrichment result,
a named list of group-level results, or a named list in which every group
contains database-level results. Each selected result must contain
\code{ID}, \code{Description}, \code{Count}, \code{pvalue}, and \code{p.adjust}.}

\item{database}{Optional database name used to select the same database from
every nested group, for example \code{"C2_CP_KEGG_LEGACY"}. Colon-form names
such as \code{"C2:CP:KEGG_LEGACY"} are also accepted. When the input tables
contain a \code{Database} column, rows are restricted to this database.}

\item{p_column}{P-value column used for top-pathway selection and point
colour. Choose \code{"p.adjust"} or \code{"pvalue"}.}

\item{pathway_column}{Pathway column used for selection and x-axis labels.
Choose \code{"ID"} (the default) or \code{"Description"}.}

\item{top_n}{Number of pathways selected independently from each group using
the smallest values in \code{p_column}. Supply one number for every group, or a
named numeric vector to set different values for different groups. Set to
\code{0} or \code{NULL} to use only \code{label_pathways}.}

\item{label_pathways}{Optional character vector containing values from the
selected \code{pathway_column}. These pathways are placed first from top to
bottom in the supplied order. Automatically selected pathways not already
listed are appended.}

\item{exclude_pathways}{Optional character vector containing values from the
selected \code{pathway_column} that must not be displayed. Exclusion is applied
before \code{top_n} selection and takes precedence over \code{label_pathways}.}

\item{group_order}{Optional character vector specifying the left-to-right
order of groups. The input list order is used by default.}

\item{sentence_case}{Logical. If \code{TRUE}, replace hyphens and underscores in
pathway labels with spaces, convert all letters to lowercase, and capitalize
only the first letter. Raw \code{ID} and \code{Description} values in the returned
table are unchanged.}

\item{low_color, high_color}{Colours representing low and high negative
log10-transformed values from \code{p_column}.}

\item{na_color}{Colour used for missing P values.}

\item{max_logp}{Optional positive numeric upper limit for the plotted
negative log10 P value. For example, \code{max_logp = 10} displays every value
greater than 10 as 10. The default \code{NULL} applies no upper limit.}
}
\value{
A named list containing \code{dotplot} and \code{table}. Point size represents
the enrichment gene count and point colour represents the negative
log10-transformed value from \code{p_column}.
}
\description{
Compares over-representation analysis results across groups using a shared
pathway axis. Pathways are selected independently within each group and then
combined, so the plotted table retains the \code{Count} and P value reported by
each group. Missing pathway-by-group combinations are retained as missing
rows and are not drawn as points.
}
