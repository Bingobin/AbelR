% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deg-plots.R
\name{plot_deg_manhattan}
\alias{plot_deg_manhattan}
\title{Draw genomic-position DEG Manhattan plots}
\usage{
plot_deg_manhattan(
  deg_list,
  color_map,
  species = c("mouse", "human"),
  deg_cols = NULL,
  symbol_col = "Symbol",
  lfc_col = "log2FoldChange",
  p_col = "pvalue",
  padj_col = "padj",
  gene_anno_file = NULL,
  chromosome_lengths = NULL,
  gene_type_filter = "protein_coding",
  remove_rik = TRUE,
  top_n = 10,
  cap_value = 20,
  chr_keep = NULL,
  facet_nrow = 1
)
}
\arguments{
\item{deg_list}{Named list of DEG data frames, one per treatment or contrast.}

\item{color_map}{Named colour vector whose names match \code{deg_list}.}

\item{species}{Species defining bundled annotation and default chromosomes.}

\item{deg_cols}{Optional four-column character vector overriding
\code{symbol_col}, \code{lfc_col}, \code{p_col}, and \code{padj_col}.}

\item{symbol_col, lfc_col, p_col, padj_col}{Column names containing gene symbol,
log2 fold change, raw P value, and adjusted P value.}

\item{gene_anno_file}{Optional compatible gene-annotation table. Bundled
annotation is used when \code{NULL}.}

\item{chromosome_lengths}{Optional named numeric vector of chromosome lengths.
Annotation maxima are used when \code{NULL}.}

\item{gene_type_filter}{Optional gene type retained when \code{Gene_Type} exists.}

\item{remove_rik}{Logical; remove mouse symbols ending in \code{Rik}.}

\item{top_n}{Number of upregulated and downregulated genes labelled per
treatment. Up to \code{top_n} genes are selected independently in each
direction.}

\item{cap_value}{Maximum absolute signed significance score displayed.}

\item{chr_keep}{Optional chromosomes and order to retain.}

\item{facet_nrow}{Number of rows in the treatment facet layout.}
}
\value{
A list with the joined data (\code{deg_merge}), labelled genes
(\code{top_genes}), and the \link[ggplot2:ggplot]{ggplot2::ggplot} object (\code{plot}).
}
\description{
Joins one or more DEG tables to human or mouse gene coordinates, calculates
a signed significance score (\code{log2FoldChange * -log10(padj)}), and draws
faceted genomic-position plots with separate top upregulated and
downregulated gene labels.
}
