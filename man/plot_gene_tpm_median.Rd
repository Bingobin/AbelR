% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deseq2.R
\name{plot_gene_tpm_median}
\alias{plot_gene_tpm_median}
\title{Plot median TPM expression for one gene}
\usage{
plot_gene_tpm_median(
  bulk_data,
  gene,
  id_col = "GID",
  group_colors = get("mycol", envir = asNamespace("AbelR"))
)
}
\arguments{
\item{bulk_data}{List returned by \code{\link[=build_bulkRNA_batches]{build_bulkRNA_batches()}} containing
\code{tpm} and \code{design} data frames.}

\item{gene}{One gene symbol or Ensembl gene ID. Ensembl version suffixes are
ignored during matching.}

\item{id_col}{Gene-identifier column in \code{bulk_data$tpm}.}

\item{group_colors}{Character vector of valid R colors. Named colors are
matched to \code{Group} levels when all levels are present; otherwise colors
are recycled in group-level order.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Uses the TPM and design tables returned by \code{\link[=build_bulkRNA_batches]{build_bulkRNA_batches()}} to
display one gene across cell lines, batches, groups, and libraries. Bars
show median \code{log2(TPM + 1)}, error bars show the interquartile range, and
points show individual samples.
}
