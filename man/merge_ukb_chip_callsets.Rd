% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ukb.R
\name{merge_ukb_chip_callsets}
\alias{merge_ukb_chip_callsets}
\title{Merge two UKB CHIP variant call sets}
\usage{
merge_ukb_chip_callsets(
  inhouse_maf,
  ukb_maf,
  by = c("Hugo_Symbol", "Chromosome", "Start_Position", "End_Position",
    "Reference_Allele", "Tumor_Seq_Allele2", "Tumor_Sample_Barcode"),
  inhouse_vaf_col = "VAF",
  ukb_vaf_col = "VAF",
  prefer = c("inhouse", "ukb", "max")
)
}
\arguments{
\item{inhouse_maf}{In-house CHIP MAF-like data frame.}

\item{ukb_maf}{UKB-released CHIP MAF-like data frame.}

\item{by}{Character vector of columns that uniquely identify a variant.}

\item{inhouse_vaf_col}{VAF column in \code{inhouse_maf}.}

\item{ukb_vaf_col}{VAF column in \code{ukb_maf}.}

\item{prefer}{Which VAF to use when both call sets contain a variant:
\code{"inhouse"}, \code{"ukb"}, or the \code{"max"} value.}
}
\value{
A merged data frame containing \code{VAF_inhouse}, \code{VAF_ukb}, combined
\code{VAF}, and \code{CHIP_source}.
}
\description{
Performs a full join of an in-house and a UKB-released CHIP call set using
explicit variant keys. Both source-specific VAF values are retained, a
preferred combined VAF is calculated, and each variant is labelled by call
source.
}
